--Complete--
# auth() in function.php
# show bookList in author detail page
# users must can add books in favorites (display added books in profile)

--New--
# TODO - указать правильный жанр (/app/routes/genres/genre_books.php:11)
# TODO - указать правильного автора (/app/routes/authors/author_books.php:11)
# Добавление в избранное:
    # 1 - отправка AJAX'ом
    # 2 - На сервере (обработчик) сделать несколько проверок (
        Общие черты => {
            1 => Если существует запрос / запрос с определенным ключом,
            2 => Какой action пришел
            3 => Ты должен проверить, есть ли такая запись в таблице (в связке с book_id и user_id)
            4 => Если запрос на добавление (
                Если есть - сразу вернуть сообщение ("Books already added to favorite")
                Если нет - добавить запись в таблицу
            )
            5 => Если запрос на удаление (
                Если есть - удалить и вернуть кнопку и форму в изначальное положение
                Если нет - вернуть кнопку в изначальное положение
            )
        
        },
        Что желательно сделать после отправки => {
            1 => скрытый инпут action поменять add на remove
        }
    )
    # 3 - При выводе книг тернарным оператором проверять является ли текущая книга у текущего пользователя в избранном (
        Для этого реализуй метод inFavourite($book_id, $user_id) в классе Favourites, которая принимает два параметра - id книги и id пользователя
        Для удобства можешь обернуть этот метод в функцию в functions.php и она будет принимать уже один параметр book_id,
        user_id ты будешь получать через класс User, методом getUserId
    )